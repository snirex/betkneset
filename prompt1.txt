כתבתי אתר אינטרנט, ואני רוצה לתקן ולשפר בו מאפיינים:
* יש בעיה עם alpine.js
* תפריט במצב מובייל לא נפתח - צריך תיקון.
* יש בעיה כשבוחרים סוג עמודה - רשימה, אז לא ניתן להגדיר את הרשימה - צריך תיקון.
* כשעוברים ללשונית כל התחייבויות או תשלומים, אז צריך שיהיה פוקוס על שדה הזנה ראשון.
* הנתונים כרגע נשמרים ב-localstorage ובעתיד יועברו לבסיס נתונים של FIREBASE. אני רוצה שתקצה שמות עם משמעות קלה להבנה עבור כל טבלה או משתנה וכאלה דברים.
* אני רוצה שיהיה דף ראשי שבו מופיעות כל התנועות האחרונות של כל המתפללים, עם אפשרות הוספה והסרה.
* אני רוצה אפשרות הגדרת צבעים לכל הדף, וליד כל אפשרות להגדרת צבע תהיה אפשרות להגדרת הצבע שיחליף ב-darkmode
* אני רוצה לעשות דף ריכוז תנועות לכל מתפלל. עמודות: תאריך, מטרת התשלום, סוג תשלום, אסמכתא, סכום התחייבות, תשלום בפעול, הפרש. 

בשורה אחרונה יוצג סיכום: "סכום התחייבות", "תשלום בפועל", הפרש. 

הפרש הוא "סכום התחייבות" פחות "תשלום בפועל".

* מחיקת שורה בכל טבלה של נתונים צריכה להיות קלה - לחצן X ואישור של לחיצת ENTER בחלונית שאלה MODAL SWEETALERT .

* מחולל דוחות - הצג דוח להדפסה, על פי מטרת תשלום בתחום תאריכים, למשל כמה תרמו לשיעור תורה כל המתפללים או מתפלל ספציפי או מתפללים ספציפיים.

* Tabulator רספונסיבי - במובייל, העמודות ש"נחנקות" יתקפלו תחת כפתור (+) קטן. 

* לכל מתפלל חדש יוקצה כינוי באנגלית בלבד, שאם לא ניתן לו מראש, אז הוא יהיה מספר בן 4 ספרות שיהיה המזהה שלו, בשביל הקצאת דפי אינטרנט בשבילו עם המספר בשורת הכתובת, למשל file:///C:/betkneset_website/try14/15.html#view=management&member=1234. בעריכת פרטי מתפלל יהיה ניתן לשנות את המספר גם לטקסט באנגלית בלבד, והשינוי יוחל על הדפים המוקצים למתפלל.
המטרה של המספר הוא שאוכל לשלוח אל המתפלל קישור לדף אינטרנט שיצג את הנתונים ששייכים אליו בלבד - למשל דף אישור תשלום, או דברים אחרים.
* תשפר את הדפסת טבלאות.
* תוסיף ייצוא של טבלאות ל-excel
* תמליץ לי על תוספים אחרים שיעזרו לי בדף ויהפכו את השימוש בו לנו יותר גם עבור המשתמש וגם עבור המפתח של הדף.

<!DOCTYPE html><html lang="he" dir="rtl" x-data="synagogueApp()" :data-bs-theme="darkMode ? 'dark' : 'light'" :style="customStyle"><head>    <meta charset="UTF-8">    <meta name="viewport" content="width=device-width, initial-scale=1.0">    <title>ניהול קהילה PRO</title>        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">    <link href="https://unpkg.com/tabulator-tables@5.5.0/dist/css/tabulator_bootstrap5.min.css" rel="stylesheet">    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">        <style>        :root { --primary-color: #0d6efd; --bg-color: #f8f9fa; --sidebar-bg: #ffffff; --sidebar-font: #333333; }        [x-cloak] { display: none !important; }        body { font-family: 'Segoe UI', system-ui, sans-serif; background-color: var(--bg-color); }                .app-container { display: flex; min-height: 100vh; width: 100vw; }        .sidebar {             width: 300px; flex-shrink: 0; border-left: 1px solid var(--bs-border-color);             background-color: var(--sidebar-bg) !important; color: var(--sidebar-font) !important;            display: flex; flex-direction: column; z-index: 1000;        }                /* Member List Scrollbar */        .member-list-container {            max-height: 50vh;            overflow-y: auto;            border-radius: 8px;        }
        .main-content { flex-grow: 1; min-width: 0; padding: 25px; height: 100vh; overflow-y: auto; }        .login-overlay { position: fixed; inset: 0; z-index: 9999; background: var(--primary-color); display: flex; align-items: center; justify-content: center; }
        /* Collapsible Cards */        .card-header { cursor: pointer; user-select: none; }        .card-header:hover { background-color: rgba(0,0,0,0.03); }
        @media (max-width: 992px) {            .sidebar { position: fixed; right: 0; transform: translateX(100%); transition: 0.3s; height: 100vh; }            .sidebar.show { transform: translateX(0); }        }
        @media print {            .no-print, .sidebar, .btn, .nav-pills, .card-header i { display: none !important; }            .main-content { padding: 0 !important; width: 100% !important; overflow: visible; }            .card-body { display: block !important; } /* Force show printed content */        }    </style></head><body>
    <div class="login-overlay no-print" x-show="!isLoggedIn" x-cloak>        <div class="card shadow-lg p-4 text-center" style="width: 350px; border-radius: 15px;">            <h3 class="mb-4">ניהול קהילה</h3>            <input type="text" x-model="loginForm.user" class="form-control mb-2" placeholder="משתמש">            <input type="password" x-model="loginForm.pass" class="form-control mb-3" placeholder="סיסמה">            <button @click="login()" class="btn btn-primary w-100 fw-bold">כניסה</button>        </div>    </div>
    <div class="app-container" x-show="isLoggedIn">                <aside class="sidebar p-3 shadow-sm no-print" :class="mobileMenu ? 'show' : ''">            <h4 class="fw-bold mb-4">מערכת קהילה</h4>                        <div class="input-group mb-2">                <input type="text" id="memberSearch" x-model="memberSearch" class="form-control" placeholder="חיפוש (ESC)">                <button class="btn btn-outline-success" @click="quickAdd = !quickAdd"><i class="bi bi-person-plus"></i></button>            </div>
            <div x-show="quickAdd" class="card p-2 mb-2 bg-light border-0" x-cloak>                <input type="text" x-model="nm.name" class="form-control form-control-sm mb-1" placeholder="שם מלא *">                <input type="text" x-model="nm.nickname" class="form-control form-control-sm mb-2" placeholder="כינוי *">                <button @click="addMember(); quickAdd = false" class="btn btn-success btn-sm w-100">שמור</button>            </div>
            <div class="member-list-container border rounded bg-white">                <div class="list-group list-group-flush">                    <template x-for="m in filteredMembers" :key="m.id">                        <button @click="selectMember(m); mobileMenu = false" class="list-group-item list-group-item-action border-0" :class="selectedMember?.id === m.id ? 'active' : ''">                            <div class="fw-bold" x-text="m.name"></div>                            <small class="opacity-75" x-text="m.nickname"></small>                        </button>                    </template>                </div>            </div>
            <div class="pt-3 border-top mt-auto">                <button @click="navigate('summary')" class="btn btn-light w-100 mb-2 text-start">📊 סיכום חובות (Alt+S)</button>                <button @click="navigate('reports')" class="btn btn-light w-100 mb-2 text-start">📄 דוחות דינמיים</button>                <button @click="navigate('management')" class="btn btn-light w-100 mb-3 text-start">⚙️ הגדרות (Alt+G)</button>                <div class="d-flex justify-content-between">                    <button class="btn btn-sm btn-outline-secondary" @click="toggleDarkMode()"><i class="bi bi-moon"></i></button>                    <button class="btn btn-sm btn-danger" @click="logout()">צא</button>                </div>            </div>        </aside>
        <main class="main-content">            <button class="btn btn-primary d-lg-none mb-3 no-print" @click="mobileMenu = !mobileMenu"><i class="bi bi-list"></i></button>
            <div x-show="view === 'account' && selectedMember" x-cloak>                <div class="card shadow-sm border-0 p-4 mb-4 d-flex flex-row justify-content-between align-items-center">                    <div>                        <h2 class="mb-0" x-text="selectedMember?.name"></h2>                        <small class="text-muted" x-text="selectedMember?.phone || selectedMember?.nickname"></small>                    </div>                    <button class="btn btn-dark no-print" onclick="window.print()"><i class="bi bi-printer"></i></button>                </div>
                <ul class="nav nav-pills mb-4 no-print shadow-sm p-1 rounded bg-body-tertiary">                    <li class="nav-item"><button class="nav-link" :class="activeTab === 'obligations' ? 'active' : ''" @click="setTab('obligations')">התחייבויות (1)</button></li>                    <li class="nav-item"><button class="nav-link" :class="activeTab === 'payments' ? 'active' : ''" @click="setTab('payments')">תשלומים (2)</button></li>                    <li class="nav-item"><button class="nav-link" :class="activeTab === 'summary' ? 'active' : ''" @click="setTab('summary')">כרטיס מלא (3)</button></li>                    <li class="nav-item"><button class="nav-link" :class="activeTab === 'edit' ? 'active' : ''" @click="setTab('edit')">עריכת פרטים (4)</button></li>                </ul>
                <div x-show="['obligations', 'payments'].includes(activeTab)">                    <div class="card p-3 mb-4 bg-body-tertiary border-0 no-print shadow-sm">                        <div class="row g-2 align-items-end" @keydown.enter="addNewEntry()">                            <template x-for="col in (activeTab === 'obligations' ? config.obColumns : config.payColumns)" :key="col.id">                                <div class="col-md">                                    <label class="small fw-bold mb-1" x-text="col.title"></label>                                    <template x-if="col.type === 'date'"><input type="date" x-model="newEntry[col.field]" class="form-control form-control-sm"></template>                                    <template x-if="col.type === 'money'"><input type="number" x-model="newEntry[col.field]" class="form-control form-control-sm"></template>                                    <template x-if="col.type === 'text'"><input type="text" x-model="newEntry[col.field]" class="form-control form-control-sm"></template>                                    <template x-if="col.type === 'list'">                                        <select x-model="newEntry[col.field]" class="form-select form-select-sm">                                            <template x-for="opt in col.options.split(',')"><option :value="opt.trim()" x-text="opt.trim()"></option></template>                                        </select>                                    </template>                                </div>                            </template>                            <div class="col-md-auto"><button @click="addNewEntry()" class="btn btn-primary btn-sm px-4">הוסף</button></div>                        </div>                    </div>                </div>
                <div x-show="activeTab === 'edit'" class="card p-4 border-0 shadow-sm">                    <h5>עריכת מתפלל</h5>                    <div class="row g-3">                        <template x-for="field in config.memberFields" :key="field.id">                            <div class="col-md-6">                                <label class="small fw-bold" x-text="field.name"></label>                                <input type="text" x-model="selectedMember[field.key]" class="form-control" @input="saveData()">                            </div>                        </template>                    </div>                </div>
                <div x-show="activeTab !== 'edit'" id="mainTable" class="bg-body shadow-sm rounded"></div>            </div>
            <div x-show="view === 'reports'" x-cloak>                <h2 class="mb-4">📄 דוחות דינמיים</h2>                <div class="card p-4 border-0 shadow-sm mb-4 no-print">                    <div class="row g-3 align-items-end">                        <div class="col-md-4">                            <label class="small fw-bold">סנן לפי ערך (למשל: "גמח"):</label>                            <input type="text" x-model="reportFilter" class="form-control" placeholder="חפש בכל הפעולות...">                        </div>                        <div class="col-md-3">                            <button @click="generateReport()" class="btn btn-primary w-100">הפק דוח</button>                        </div>                    </div>                </div>                <div id="reportTable" class="bg-body shadow-sm rounded"></div>            </div>
            <div x-show="view === 'management'" x-cloak>                <div class="row g-4">                    <div class="col-md-7">                        <div class="card border-0 shadow-sm mb-3">                            <div class="card-header bg-transparent d-flex justify-content-between" @click="ui.obOpen = !ui.obOpen">                                <h6 class="mb-0">עמודות התחייבויות</h6>                                <i class="bi" :class="ui.obOpen ? 'bi-chevron-up' : 'bi-chevron-down'"></i>                            </div>                            <div class="card-body" x-show="ui.obOpen" x-transition>                                <div class="list-group mb-2">                                    <template x-for="(col, index) in config.obColumns" :key="col.id">                                        <div class="list-group-item d-flex gap-2 p-1">                                            <input type="text" x-model="col.title" class="form-control form-control-sm w-50">                                            <select x-model="col.type" class="form-select form-select-sm w-25">                                                <option value="text">טקסט</option><option value="money">כסף</option><option value="date">תאריך</option><option value="list">רשימה</option>                                            </select>                                            <button @click="config.obColumns.splice(index, 1)" class="btn btn-sm text-danger" :disabled="col.fixed"><i class="bi bi-trash"></i></button>                                        </div>                                    </template>                                </div>                                <button @click="addColumn('ob')" class="btn btn-sm btn-outline-primary">הוסף עמודה</button>                            </div>                        </div>
                        <div class="card border-0 shadow-sm mb-3">                            <div class="card-header bg-transparent d-flex justify-content-between" @click="ui.payOpen = !ui.payOpen">                                <h6 class="mb-0">עמודות תשלומים</h6>                                <i class="bi" :class="ui.payOpen ? 'bi-chevron-up' : 'bi-chevron-down'"></i>                            </div>                            <div class="card-body" x-show="ui.payOpen">                                <div class="list-group mb-2">                                    <template x-for="(col, index) in config.payColumns" :key="col.id">                                        <div class="list-group-item d-flex gap-2 p-1">                                            <input type="text" x-model="col.title" class="form-control form-control-sm w-50">                                            <button @click="config.payColumns.splice(index, 1)" class="btn btn-sm text-danger" :disabled="col.fixed"><i class="bi bi-trash"></i></button>                                        </div>                                    </template>                                </div>                                <button @click="addColumn('pay')" class="btn btn-sm btn-outline-success">הוסף עמודה</button>                            </div>                        </div>
                        <div class="card border-0 shadow-sm mb-3">                            <div class="card-header bg-transparent d-flex justify-content-between" @click="ui.fieldsOpen = !ui.fieldsOpen">                                <h6 class="mb-0">הגדרת שדות מתפלל</h6>                                <i class="bi" :class="ui.fieldsOpen ? 'bi-chevron-up' : 'bi-chevron-down'"></i>                            </div>                            <div class="card-body" x-show="ui.fieldsOpen">                                <template x-for="(f, i) in config.memberFields" :key="f.id">                                    <div class="d-flex gap-2 mb-2">                                        <input type="text" x-model="f.name" class="form-control form-control-sm">                                        <button @click="config.memberFields.splice(i, 1)" class="btn btn-sm text-danger" :disabled="f.required"><i class="bi bi-trash"></i></button>                                    </div>                                </template>                                <button @click="addMemberField()" class="btn btn-sm btn-outline-primary">הוסף שדה חדש</button>                            </div>                        </div>                    </div>
                    <div class="col-md-5">                        <div x-show="editingMember" class="card p-4 border-0 shadow-sm mb-4 bg-primary-subtle border-primary">                            <div class="d-flex justify-content-between align-items-center mb-3">                                <h5 class="mb-0">עריכת מתפלל</h5>                                <button class="btn-close" @click="editingMember = null"></button>                            </div>                            <template x-for="field in config.memberFields" :key="field.id">                                <div class="mb-2">                                    <label class="small fw-bold" x-text="field.name"></label>                                    <input type="text" x-model="editingMember[field.key]" class="form-control form-control-sm">                                </div>                            </template>                            <button @click="updateMember()" class="btn btn-primary btn-sm w-100 mt-2">עדכן ושמור</button>                        </div>
                        <div class="card border-0 shadow-sm">                            <div class="card-header bg-transparent"><h6>רשימת מתפללים לעריכה</h6></div>                            <div class="card-body p-0">                                <div class="list-group list-group-flush" style="max-height: 300px; overflow-y: auto;">                                    <template x-for="m in members" :key="m.id">                                        <div class="list-group-item d-flex justify-content-between align-items-center p-2">                                            <span x-text="m.name"></span>                                            <div>                                                <button @click="editingMember = {...m}" class="btn btn-sm btn-outline-primary"><i class="bi bi-pencil"></i></button>                                                <button @click="deleteMember(m.id)" class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button>                                            </div>                                        </div>                                    </template>                                </div>                            </div>                        </div>                    </div>                </div>            </div>
            <div x-show="view === 'summary'" x-cloak>                <div class="d-flex justify-content-between align-items-center mb-4">                    <h2>📊 סיכום חובות כללי</h2>                    <button class="btn btn-dark no-print" onclick="window.print()"><i class="bi bi-printer"></i></button>                </div>                <div id="summaryTable" class="bg-body shadow-sm rounded"></div>            </div>        </main>    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>    <script src="https://unpkg.com/tabulator-tables@5.5.0/dist/js/tabulator.min.js"></script>    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <script>    function synagogueApp() {        return {            isLoggedIn: localStorage.getItem('syn_auth') === 'true',            loginForm: { user: '', pass: '' },            view: 'account', activeTab: 'obligations',            selectedMember: null, editingMember: null,            memberSearch: '', quickAdd: false, reportFilter: '',            darkMode: localStorage.getItem('syn_dark_mode') === 'true',            nm: { name: '', nickname: '' },            ui: { obOpen: true, payOpen: true, fieldsOpen: true },
            config: JSON.parse(localStorage.getItem('syn_conf_v6')) || {                obColumns: [                    { id: 'ob1', title: 'תאריך', field: 'date', type: 'date', fixed: true },                    { id: 'ob3', title: 'סכום', field: 'amount', type: 'money', fixed: true }                ],                payColumns: [                    { id: 'pa1', title: 'תאריך', field: 'date', type: 'date', fixed: true },                    { id: 'pa3', title: 'סכום', field: 'amount', type: 'money', fixed: true }                ],                memberFields: [                    { id: 'm1', name: 'שם מלא', key: 'name', required: true },                    { id: 'm2', name: 'כינוי', key: 'nickname', required: true },                    { id: 'm3', name: 'טלפון', key: 'phone', required: false }                ],                theme: { primary: '#0d6efd', bg: '#f8f9fa', sidebarBg: '#ffffff', sidebarFont: '#333333' }            },
            members: JSON.parse(localStorage.getItem('syn_mem_v6')) || [],            transactions: JSON.parse(localStorage.getItem('syn_tx_v6')) || [],            newEntry: {},
            init() {                this.resetEntry();                this.handleRouting();                this.$watch('config', (v) => { localStorage.setItem('syn_conf_v6', JSON.stringify(v)); this.renderTable(); }, { deep: true });                                window.addEventListener('keydown', (e) => {                    if (!this.isLoggedIn) return;                    if (e.key === 'Escape') { e.preventDefault(); document.getElementById('memberSearch').focus(); }                    if (e.altKey && e.key === 's') this.navigate('summary');                    if (e.altKey && e.key === 'g') this.navigate('management');                    if (e.altKey && e.key === '1') this.setTab('obligations');                    if (e.altKey && e.key === '2') this.setTab('payments');                    if (e.altKey && e.key === '3') this.setTab('summary');                    if (e.altKey && e.key === '4') this.setTab('edit');                });                window.addEventListener('hashchange', () => this.handleRouting());            },
            handleRouting() {                const params = new URLSearchParams(window.location.hash.substring(1));                if (params.get('view')) this.view = params.get('view');                if (params.get('member')) this.selectedMember = this.members.find(m => m.id == params.get('member'));                if (params.get('tab')) this.activeTab = params.get('tab');                setTimeout(() => this.renderTable(), 100);            },
            updateURL() {                let h = `view=${this.view}`;                if (this.selectedMember) h += `&member=${this.selectedMember.id}`;                if (this.view === 'account') h += `&tab=${this.activeTab}`;                window.location.hash = h;            },
            navigate(v) { this.view = v; this.updateURL(); },            setTab(t) { this.activeTab = t; this.updateURL(); },            selectMember(m) { this.selectedMember = m; this.view = 'account'; this.updateURL(); },
            get customStyle() { return `--primary-color: ${this.config.theme.primary}; --bg-color: ${this.config.theme.bg}; --sidebar-bg: ${this.config.theme.sidebarBg}; --sidebar-font: ${this.config.theme.sidebarFont};`; },            login() { if(this.loginForm.user === 'admin' && this.loginForm.pass === '1234') { this.isLoggedIn = true; localStorage.setItem('syn_auth', 'true'); } },            logout() { this.isLoggedIn = false; localStorage.removeItem('syn_auth'); window.location.hash = ''; },            toggleDarkMode() { this.darkMode = !this.darkMode; localStorage.setItem('syn_dark_mode', this.darkMode); },
            get filteredMembers() { return this.members.filter(m => m.name.includes(this.memberSearch)); },            addMember() {                const m = { id: Date.now(), name: this.nm.name, nickname: this.nm.nickname };                this.config.memberFields.forEach(f => m[f.key] = m[f.key] || '');                this.members.push(m); this.saveData(); this.selectMember(m);            },            updateMember() {                 const idx = this.members.findIndex(m => m.id === this.editingMember.id);                this.members[idx] = {...this.editingMember}; this.saveData(); this.editingMember = null;             },            deleteMember(id) { if(confirm('למחוק מתפלל?')) { this.members = this.members.filter(m => m.id !== id); this.transactions = this.transactions.filter(t => t.memberId !== id); this.saveData(); this.selectedMember = null; this.updateURL(); } },
            addColumn(type) {                 const list = type === 'ob' ? this.config.obColumns : this.config.payColumns;                list.push({ id: 'f'+Date.now(), title: 'חדש', field: 'f'+Date.now(), type: 'text' });             },            addMemberField() { this.config.memberFields.push({ id: 'mf'+Date.now(), name: 'שדה חדש', key: 'k'+Date.now(), required: false }); },
            resetEntry() {                const cols = this.activeTab === 'obligations' ? this.config.obColumns : this.config.payColumns;                cols.forEach(c => this.newEntry[c.field] = (c.type === 'date' ? new Date().toISOString().split('T')[0] : ''));            },
            addNewEntry() {                if(!this.newEntry.amount) return;                this.transactions.unshift({ id: Date.now(), memberId: this.selectedMember.id, kind: this.activeTab, ...this.newEntry });                this.saveData(); this.resetEntry(); this.renderTable();            },
            saveData() { localStorage.setItem('syn_mem_v6', JSON.stringify(this.members)); localStorage.setItem('syn_tx_v6', JSON.stringify(this.transactions)); },
            renderTable() {                if (this.view === 'summary') { this.renderGlobalSummary(); return; }                if (this.view === 'reports') { this.generateReport(); return; }                if (!this.selectedMember || this.view !== 'account' || this.activeTab === 'edit') return;
                const curCols = this.activeTab === 'obligations' ? this.config.obColumns : this.config.payColumns;                let cols = curCols.map(c => ({                     title: c.title, field: c.field, editor: true,                     formatter: c.type === 'money' ? 'money' : undefined,                    editorParams: c.type === 'list' ? { values: c.options.split(',').map(o => o.trim()) } : {}                }));
                let data = this.transactions.filter(t => t.memberId === this.selectedMember.id);                if (this.activeTab === 'summary') {                    let bal = 0;                    let sData = [...data].sort((a,b) => a.id - b.id).map(t => {                        bal += (t.kind === 'obligations' ? Number(t.amount) : -Number(t.amount));                        return {...t, bal: bal};                    });                    new Tabulator("#mainTable", { data: sData.reverse(), layout: "fitColumns", columns: [{title:"תאריך", field:"date"}, {title:"יתרה", field:"bal", formatter:"money", cssClass:"fw-bold"}] });                } else {                    new Tabulator("#mainTable", { data: data.filter(t => t.kind === this.activeTab), layout: "fitColumns", columns: cols, cellEdited: () => this.saveData() });                }            },
            generateReport() {                if(!this.reportFilter) return;                const f = this.reportFilter.toLowerCase();                const data = this.transactions.filter(t => {                    return Object.values(t).some(val => String(val).toLowerCase().includes(f));                }).map(t => {                    const m = this.members.find(mem => mem.id === t.memberId);                    return { ...t, memberName: m ? m.name : 'לא ידוע' };                });
                new Tabulator("#reportTable", {                    data, layout: "fitColumns",                    columns: [                        {title: "מתפלל", field: "memberName"},                        {title: "תאריך", field: "date"},                        {title: "סוג", field: "kind", formatter: c => c.getValue() === 'obligations' ? 'חיוב' : 'תשלום'},                        {title: "סכום", field: "amount", formatter: "money"}                    ]                });            },
            renderGlobalSummary() {                const data = this.members.map(m => {                    const tx = this.transactions.filter(t => t.memberId === m.id);                    const bal = tx.reduce((s, t) => s + (t.kind === 'obligations' ? Number(t.amount) : -Number(t.amount)), 0);                    return { name: m.name, bal: bal };                });                new Tabulator("#summaryTable", { data, layout: "fitColumns", columns: [{title: "שם", field: "name", widthGrow: 2}, {title: "חוב", field: "bal", formatter: "money", bottomCalc: "sum"}] });            }        }    }    </script></body></html>
